# oh-my-n8n 커뮤니티 플러그인 화이트리스트 (SSOT).
# 워크플로 작성 에이전트는 여기 등록된 노드 + custom-nodes/ 만 사용 가능.
# 변경 시 `scripts/sync-plugins.mjs` 실행 → plugins/install.sh + lockfile.json 갱신.

n8nVersion: ">=2.15.0 <3.0.0"

plugins:
  # 예시: 실제 사용 시 조직 표준에 맞게 추가/삭제
  # - name: n8n-nodes-mcp
  #   version: ^0.5.0
  #   reason: MCP 서버 연동
  # - name: n8n-nodes-puppeteer
  #   version: ^1.4.0
  #   reason: 헤드리스 브라우저 자동화
